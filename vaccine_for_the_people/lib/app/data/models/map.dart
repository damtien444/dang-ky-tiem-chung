import 'package:flutter/material.dart';

class Model {
  Model(this.state, this.color);
  String state;
  Color color;
}
class Model1 {
  Model1(this.state, this.color);
  String state;
  Color color;
}